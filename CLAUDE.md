# CLAUDE.md — Joy Factory aXtion Product Configurator

**Project:** Joy Factory aXtion guided selling configurator  
**Stack:** Next.js 14+ (App Router), TypeScript, Tailwind CSS v4  
**AI:** Anthropic Claude API (claude-sonnet-4-6) — server-side only  
**Integrations:** BigCommerce Storefront API, HubSpot Forms API  
**Deployment target:** Vercel  

> Historical build instructions (Phase 1 & 2 step-by-step) are archived in `docs/BUILD_REFERENCE.md`.

---

## WORKING PRINCIPLES

General engineering discipline for this repo — applies on top of the project-specific rules below. Use judgment on trivial tasks; bias toward caution on anything nontrivial.

1. **Think before coding.** State assumptions explicitly. If multiple interpretations exist, present them — don't pick silently. If a simpler approach exists, say so. If something is unclear, stop and ask rather than guessing.

2. **Simplicity first.** Minimum code that solves the problem — no speculative features, no unrequested abstractions or configurability, no error handling for impossible scenarios. If it could be a quarter of the size, rewrite it.

3. **Surgical changes.** Touch only what the task requires. Don't refactor or "improve" adjacent code, comments, or formatting, and don't touch unrelated dead code — mention it instead of removing it. Match existing style even where you'd do it differently. Do remove imports/variables/functions that your own change made unused.

4. **Goal-driven execution.** Turn tasks into verifiable success criteria before starting (e.g. "fix the bug" → "write a test that reproduces it, then make it pass"). For multi-step work, state a brief plan with a verification check per step, then loop against it rather than stopping to ask at each step.

Every changed line should trace back to the actual request. If a diff drifts into unrelated refactoring or unrequested flexibility, that's the signal to stop and reconsider.

---

## CURRENT STATUS — PHASE 2 COMPLETE + CONTACT FORM + SHARE BUNDLE WORKING

**All phases complete and working.** Do not delete or rebuild from scratch. Read this section before making any changes.

### What's done
- All 5 steps + Contact Sales fully navigable
- Real Claude API call wired in (`/api/claude` → StepBundle "Why this bundle fits" section)
- BigCommerce cart API route implemented (`/api/cart`)
- **Checkout redirect escapes the iframe** — `ConfiguratorShell.tsx`'s `handleAddToCart` navigates via `(window.top || window).location.href = checkoutUrl` instead of `window.location.href`. Found when embedding the app on a HubSpot landing page (`info.thejoyfactory.com`) via iframe: BC's checkout relies on a `SF-CSRF-TOKEN` cookie set `SameSite=Strict`, which browsers refuse to set/send inside a nested third-party iframe — redirecting only the iframe left checkout unable to establish its session ("unable to connect"). Writing to `window.top.location` is permitted cross-origin (unlike reading it) and equals `window` itself when the app isn't embedded, so this is a no-op change for the normal non-embedded case. Verified with Playwright: simulated an iframe-embedded flow and confirmed the **top-level** test page (not just the iframe) navigated to the real BC checkout URL on Add to cart.
- embed.js widget in `/public/embed.js` for drop-in iframe embedding
- **Side-panel opening style** — `embed.js`'s FAB trigger button now opens a full-height panel that slides in from the left or right edge of the viewport (`data-width`/`data-height` defaults: `25vw` / `100vh`, i.e. a quarter-width panel spanning the full screen height) instead of a small box anchored near the FAB. (An earlier iteration built this as a full-width panel dropping from the top instead — corrected once it became clear "top to bottom" meant full height, not a horizontal bar.) The panel (`.agc-frame-container`) is appended directly to `<body>`, decoupled from the FAB's own corner container, and picks its side automatically from `data-position` (`side = position.includes('left') ? 'left' : 'right'`) so it always opens on the same side as the FAB rather than underneath it — `.agc-side-left`/`.agc-side-right` control which edge it's anchored to and which direction it slides in from (`translateX(-100%/0)` vs `translateX(100%/0)`). FAB keeps a higher z-index than the panel so it stays visible/clickable (to close) above it. Verified with Playwright for both the default (`bottom-right` → right-anchored) and `data-position="bottom-left"` (→ left-anchored) cases: full 800px viewport height, ~25vw width, correct anchor edge, and the app loads and renders correctly inside.
- **`embed.js` mobile icon-only FAB** — on phone-width screens (`@media (max-width: 640px)`, same breakpoint as the mobile full-width panel below) the FAB always renders as the compact 56px icon-only circle, never the "Bundle Builder" text pill, regardless of open/closed state. The wider pill was overlapping/blocking other bottom-right page widgets on mobile (e.g. a chat widget's send button) that the desktop-width pill had room to avoid. Desktop/tablet above 640px is unaffected — still the labeled pill, collapsing to icon-only only while the panel is open. Verified with Playwright: 375px viewport → FAB renders at 56px with label hidden; 1200px viewport → FAB stays a ~168px pill with the label visible.
- **`embed.js` FAB restyled as a chat-launcher pill (sparkle icon + label + send button)** — the floating button was redesigned to match the site's existing "Ask me anything" AI chat launcher: a sparkle icon (`.agc-fab-sparkle`, 8-point star path) + the "Bundle Builder" label (`.agc-fab-label`, dark gray `#44403c`), and a small red circular "send" button (`.agc-fab-send`, white right-arrow icon) tucked into its right edge. Icon/label visibility for every state is driven entirely by CSS off the `.agc-active` class and the `max-width: 640px` media query — `toggleWidget()` in the JS was simplified to a plain `classList.toggle`, removing the old manual `iconOpen`/`iconClose` `style.display` toggling this replaced. The old "text-only pill above phone width" and "laptop two-line stacking" rules (both specific to the previous plain icon+label design) were removed as part of this rewrite — no longer applicable now that the idle state is always the full chat-bar-style pill outside of mobile.
- **Fix: `embed.js` FAB shape overridden by the site's global Elementor button CSS** — the pill wasn't rendering with the intended rounded shape live on WordPress; DevTools showed why: `.elementor-kit-859 button` (a broad theme-wide rule targeting every `<button>`) sets its own `border-radius: 20px`, and since `.agc-fab`'s `border-radius` never carried `!important` (only `background`/`border`/`color` did, from the earlier data-target color-bleed fix), the theme rule's higher specificity was winning for radius specifically — `background` was correctly overridden (struck through in DevTools) while `border-radius` wasn't. Fixed by adding `!important` to `.agc-fab`'s `border-radius` and setting it to `20px` to match the site's own convention (rather than guessing a different value) — sets up a nice side effect too: the site's other buttons and Bundle Builder now share the same corner rounding. The two collapsed-circle states (`.agc-fab.agc-active`, the mobile-forced circle) each got their own `border-radius: 22px !important` override, since 20px isn't enough to fully round a 44×44 box into a true circle — 22px (exactly half) is. Verified with Playwright against a simulated `.elementor-kit-859 button { border-radius: 20px }` conflict: idle pill computes to `20px` (not clobbered to something else), collapsed/active circle computes to `22px` and renders as a true circle.
- **`embed.js` FAB — send button changed from a floating circle badge to a seamless pill end-cap** — per follow-up feedback that the button didn't read as one cohesive "pill" shape (a white pill with a separate red circle floating inside it, from the previous change below, looked like two overlapping shapes rather than one). `.agc-fab` gained `overflow: hidden` and its right padding dropped to `0` (`padding: 4px 0 4px 16px`); `.agc-fab-send` changed from a fixed 30×30 circle (`border-radius: 999px`, small margin) to a flush end-cap — `width: 40px`, `align-self: stretch` (fills the button's full inner height instead of a fixed 30px), `margin-left: auto` (pushes it to the pill's right edge), no border-radius of its own. The parent's `overflow: hidden` + its own `border-radius: 22px` clip the flush rectangular cap into the exact same curve as the pill's own rounded end, so the whole button now reads as one continuous two-tone pill (white body, red cap) rather than a pill containing a separate circle badge. Sparkle icon, label, and send arrow icon are all otherwise unchanged. Verified with Playwright (zoomed screenshot of just the button): the cap's corner cleanly follows the pill's curve with no visible seam/notch; active (panel-open) and mobile-collapsed circle states are unaffected.
- **`embed.js` FAB — removed red background, white pill only** — the first version of the chat-launcher redesign (above) wrapped the pill in a solid red outer background (`.agc-fab { background: #c8291c }`) with a separate white inner pill (`.agc-fab-inner`) nested inside, matching the reference image literally. Per follow-up feedback, the red background/outer-pill layer was removed entirely — `.agc-fab` itself is now the (only) white pill (`background: white`, a subtle `rgba(0,0,0,0.08)` border + soft shadow for definition against the page instead of red fill), and `.agc-fab-inner` was deleted, folding its children (sparkle icon, label, send button) directly into `.agc-fab-idle`. Padding changed from the old inner pill's asymmetric `0 4px 0 14px` to `4px 4px 4px 16px` on the outer button with `gap: 8px` in the flex row, giving the label an equal 8px gap to the icon on one side and the send button on the other — "balancing" the text instead of it sitting cramped near one edge. Hover changed from a solid darker-red background fill to a red border highlight (`border-color: #c8291c`) plus the existing lift/shadow, since there's no longer a colored fill to darken. The collapsed-circle states (`#agc-icon-collapsed`, `#agc-icon-close`) switched from white icons on a red circle to red icons (`color: #c8291c`) on a white circle, matching the new white-only palette — the circle's background comes from the same base `.agc-fab` white/border/shadow rule now, no per-state color override needed. Verified with Playwright: idle background computes to white, and screenshots confirm balanced padding, the red-border hover state, and a clean white circle with red ✕ when the panel opens.
- **Reverted: `embed.js` back to default floating bottom-right, header placement removed** — after presenting the header-inline placement to the company, the decision was to go back to the default floating bottom-right corner widget instead. No code change was needed for the position itself — the WPCode snippet was simply never given a `data-target` attribute in this deployment, so it already falls through to the default floating-corner behavior (`data-target`/header-inline placement code in `embed.js` is untouched and still available if it's wanted again later — just not currently used). The two-line "laptop" text-stacking behavior (previously `@media (min-width: 641px) and (max-width: 1560px)`, tuned through 1400px → 1428px → 1434px → 1475px → 1499px → 1524px → 1560px to match where the header nav piled up) was removed entirely, since it existed solely to keep the button narrow enough not to crowd the header nav — irrelevant now that the button floats independently in the corner again. Verified with Playwright: with no `data-target` set, the button renders as the floating widget and stays a consistent single-line 140×42 pill at 1400px, 1500px, and 1800px (no stacking).
- **Fix: `embed.js` FAB idle state showing red-on-red on the live WordPress header** — after the white/red outline redesign (below), the button rendered correctly everywhere tested locally, but on the live `data-target` (header-inline) placement the idle state showed a red background behind the red text — the host page's own button styling (Elementor/theme CSS) was winning the cascade against `.agc-fab`'s single-class-specificity rules, since in `data-target` mode the button is a bare `<button>` sitting directly in the host page's own markup, fully exposed to its styles (unlike floating-corner mode, which is more insulated). Fixed by adding `!important` to `.agc-fab`'s `background`/`border`/`color`, `.agc-fab:hover`'s `background`/`color`, and `.agc-fab-label`'s `color: inherit`. Verified with Playwright against a simulated conflicting host rule (`header button { background-color: red; color: red }`): background/text now correctly stay white/red regardless.
- **`embed.js` FAB height matched to header CTAs + red hover-invert** — `.agc-fab` height/radius went from `56px`/`28px` to `48px`/`24px` (later further reduced to `42px`/`21px`, see below) to match the site's "Contact Sales"/"Shop Now" header buttons (the icon-only collapsed states — `.agc-active` and the mobile-forced circle — were updated alongside, so they stay perfect circles at each size, not ovals). Background went from the blue gradient to `white` with a `1.5px solid #c8291c` border (brand red, same token as `--color-brand`) — an outline-style pill. `color: #c8291c` on `.agc-fab` drives both the label (`.agc-fab-label { color: inherit }`) and the icon (`currentColor` stroke) together; `.agc-fab:hover` fills the background solid red and flips `color` to white, so label and icon invert together on hover. The collapsed circle states (mobile idle, panel-open) read as a white circle with a red outline/icon — verified legible against a white header background.
- **`embed.js` FAB height reduced to 42px** — further reduced from `48px` per follow-up feedback: `border-radius` 24px→21px, and every place width was matched to height to keep the collapsed circle states round (`.agc-fab.agc-active`, the `@media (max-width: 640px)` mobile-forced circle, and the laptop-stacking block's `min-height`) updated from `48px` to `42px` alongside. Icon size (`.agc-fab svg`, `24px`) was left unchanged and still fits comfortably in the smaller circle. Verified with Playwright across all states: idle pill 140×42, collapsed/active circle 42×42, mobile icon-only 42×42, laptop-stacked auto-height (~52px for two lines) unaffected since it was already independent of the base height.
- **`embed.js` FAB font matched to site (Poppins)** — `.agc-fab` now sets `font-family: 'Poppins', -apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, sans-serif` directly (not just on the outer `.agc-widget-container`, which `data-target`/header mode bypasses entirely by appending the button straight into the host page). Since the FAB is real DOM injected into the host page (not inside the iframe), it inherits whichever `Poppins` the host site already loads for its other header buttons — no font loading needed on our end. Verified computed `font-family` resolves to `Poppins, ...` in `data-target` mode.
- **`embed.js` text-only pill above phone width** — a `@media (min-width: 641px)` rule hides `#agc-icon-open` (with `!important`, since the open/close toggle sets that icon's `display` as an inline style) and evens out the padding to `0 20px` whenever the FAB is idle (`:not(.agc-active)`) — shortening the "Bundle Builder" pill to text-only, matching plain-text buttons it sits alongside (e.g. header CTAs like "Contact Sales"/"Shop Now" via `data-target`). Scoped narrowly: the close (X) icon shown while the panel is open, and the icon-only circle forced on phone widths (no room for label text there), are both untouched and keep their icon. Verified with Playwright: desktop idle state hides the icon and shrinks (168px → ~140px); desktop open state still shows the close icon at 56px; mobile idle state still shows the icon (no label to fall back on there).
- **`embed.js` mobile corner flip (bottom-right → bottom-left)** — shrinking the FAB to icon-only on mobile (above) wasn't enough on its own: the site's third-party chat widget docks its own launcher/send button in that same bottom-right corner, so any button there collides with it once the chat panel is open, blocking its submit control. Same `@media (max-width: 640px)` block now also overrides `.agc-pos-bottom-right` to `left: 20px` (instead of `right: 20px`) on mobile only, relocating the trigger button to the bottom-left where nothing else on the page docks. Only the small trigger button's corner moves — the slide-in panel's own edge/side (driven by `data-position`/`side`, unrelated CSS class) is untouched, so it still opens from the same edge as before. `.agc-pos-bottom-left` (if ever explicitly configured via `data-position`) is unaffected by this override, already being on the left. Desktop/tablet above 640px keeps the original bottom-right placement. Verified with Playwright: 375px viewport → FAB relocates to `left: 20px` and the panel still opens correctly on click; 1200px viewport → FAB stays at the right edge, unchanged.
- **`embed.js` mobile full-width panel** — a `@media (max-width: 640px)` rule (same breakpoint convention as `embed-inline.js`'s mobile full-bleed fix) overrides `.agc-frame-container` to `width: 100vw` on phone-width screens, regardless of the configured `data-width` (25vw by default). `border-radius`/side border are also zeroed out at that breakpoint since the panel now reaches both screen edges instead of stopping partway across, so no stray rounded corners peek through. Desktop/tablet sizing above 640px is untouched. Verified with Playwright: 375px viewport → panel renders at the full 375px width; 1400px viewport → panel stays at ~25vw (351px), unchanged.
- **`embed.js` FAB labeled + repositioned** — the floating trigger button is now a labeled pill ("Bundle Builder" text next to the icon, via `.agc-fab-label`) instead of an icon-only circle, so users know what it opens without having to click it first. Configurable via `data-label` (defaults to `"Bundle Builder"`); the label is set with `textContent`, not string-interpolated into `innerHTML`, so a `data-label` value can never inject markup. The button collapses back to a plain 56px icon-only circle (`.agc-fab.agc-active`, label hidden) once the panel is open, matching the existing open/close icon-swap behavior. Also nudged the floating corner position down (`.agc-pos-bottom-right`/`.agc-pos-bottom-left` `bottom: 20px` → `10px`) so it sits with more clearance from the app's own in-panel "Next step" button, which shares the same bottom-right corner. Verified with Playwright: label text renders and reads "Bundle Builder", hides correctly on open, FAB collapses to ~56px width when active, and the corner offset shifted as expected.
- **`embed.js` inline/targeted FAB placement (`data-target`)** — same `data-target` CSS-selector pattern already used by `embed-inline.js`, added to `embed.js` so the trigger button can be pinned inside a specific page element (e.g. one particular header template/region) instead of always floating fixed in a viewport corner. When `data-target` resolves to an element on the page, the bare `<button>` (`.agc-fab`) is appended directly into it — no `.agc-widget-container` wrapper, no fixed positioning/corner offsets — so it sits in normal document flow wherever that element is. `data-position` still controls which edge the slide-in panel opens from (`side`) even in this mode, decoupled from where the button itself visually sits, since the panel (`.agc-frame-container`) is always appended to `<body>` and fixed to the viewport edge regardless of FAB placement. No `data-target` set = unchanged floating-corner behavior. Use case: placing the button inside one specific WordPress header template/region (e.g. a region-specific header) rather than site-wide.
- **Fix: `embed.js` no longer falls back to a floating corner button when `data-target` is missing** — previously, if the `data-target` selector wasn't found on the page, the script fell back to rendering the default floating bottom-right button instead. The WPCode snippet runs site-wide via Header & Footer, but only the USA header template (TJF-Header) has the `#axtion-config-btn` anchor div — every other locale (`/apac`, `/tw`, `/jp`) and any other header lacks it, so those pages were all incorrectly showing the floating widget. Changed to bail out entirely (`return` before any DOM/styles are created) when `data-target` is configured but not found, logging a `console.warn` instead of falling back. Floating mode is still the default for any deployment that never sets `data-target` in the first place. Verified with Playwright: a page without the anchor div now renders zero `.agc-fab`/`.agc-frame-container`/injected `<style>` elements; a page with the anchor div is unaffected.
- **Inline embed option** — `/public/embed-inline.js` is a second embed script for pages that want the full app embedded directly in their layout, not a floating widget button. It builds an `<iframe src="{baseUrl}/?embed=true">` sized via `data-width`/`data-height`/`data-max-width` attributes (defaults: `100%` / `900px` / `1500px`) — no FAB, no open/close toggle, no `position` fixed styling. By default it drops the iframe in-place where its own `<script>` tag sits (same self-discovery pattern as `embed.js`, so both can coexist on a page). Optional `data-target="{CSS selector}"` renders into a specific container element instead — needed for CMS/page-builder embeds (Webflow, WordPress, Squarespace, etc.) that relocate injected `<script>` tags to the page head/footer rather than preserving their authored position, so relying on script position alone isn't reliable there. Verified with Playwright: renders inline between surrounding host-page content with no target set, and correctly lands inside a named container div even when the script tag itself sits far away (e.g. at the bottom of `<body>`) with `data-target` set.
- **Mobile full-bleed for inline embed** — `embed-inline.js` injects a one-time `<style>` block with a `@media (max-width: 640px)` rule that overrides the iframe's width to `100vw` with negative side margins (`calc(50% - 50vw)`) on narrow screens, breaking it out of whatever left/right padding the host page's own container/column applies (common on HubSpot/CMS page builders) so the app fills the phone screen edge-to-edge instead of showing side gutters. Desktop/tablet sizing (`data-width`/`data-max-width`) is untouched above 640px. Verified with Playwright against a simulated HubSpot-style padded container: 0px gutters on both sides at a 375px viewport (previously ~40px each side from the container's own padding), no change at 1200px viewport.
- **Live BC bundle** — `/api/bundle` fetches all BC products, filters cases by `device_compatibility`, scores mounts/accessories via enrichment data, returns up to 2 `BundleOption[]` with real BC product IDs
- **Enrichment layer** — `src/lib/enrichment.ts` is the recommendation control file. Maps SKU → `{ mount_surface[], features[], series, bundle_priority }`. Scoring uses this first; keyword matching is the fallback for any SKU not in the map.
- **Claude enrichment** — `src/lib/claudeEnrichment.ts` + `POST /api/admin/enrich` auto-generates enrichment for all BC products. Re-run when new SKUs are added.
- **No-products handling** — if BC has no cases for a selected device, Review step shows a clear message and routes to Contact Sales instead of silently showing placeholder data
- **BC IDs end-to-end** — `BundleItem.bcProductId`/`bcVariantId` from BC flow through to the cart route
- **Scenario-implied features** — `/api/bundle` derives implied features from environment answers before scoring accessories: iPhone `carry_style: holster/hand` → implies `hand_strap`; `hands_free: yes` (both iPhone and tablet) → implies `shoulder_strap`. These implied features are merged with explicitly selected features for accessory scoring only.
- **Device-specific accessory priority** — accessory pool is split into Tier 1 (BC `device_compatibility` explicitly lists the device) and Tier 2 (universal, no `device_compatibility`). Tier 1 is used when available, preventing tablet-only accessories (e.g. shoulder strap) from winning iPhone bundles where device-specific accessories exist (e.g. CPX302 Belt Clip Holster).
- **iPhone `hand_strap` removed from Features step** — `hand_strap` is not a selectable feature for iPhone users; it is implied automatically via the `carry_style` environment question instead.
- **HD mount priority** — all 7 HD mount SKUs (MMU232, MMU332, MVU232, MVU332, MMU230, MMU231, MMU331) have `bundle_priority: 1` in enrichment.ts. `scoreMount` uses `bundle_priority` as a fractional tiebreaker (same pattern as cases), so HD mounts beat non-HD mounts with the same `mount_surface` every time.
- **`solution_type` scoring** — `scoreMount` in `/api/bundle` reads the BC `solution_type` custom field (Drill Down, Adhesive, Rail/Pole) and applies a bonus score when the user's `mount_install` answer matches: adhesive preference → +2 to adhesive mounts; drill preference → +2 to drill-only mounts (VESA), +1 to drill+adhesive combo mounts; rail → +2 to Rail/Pole mounts. For vehicle and pole surfaces, `mount_install` is inferred automatically (vehicle → drill, pole → rail) so no question is shown.
- **`mount_install` environment question** — conditional tablet question "How will the mount attach to the surface?" with choices Permanent/drill-down and Adhesive/no-drill. Only shown when `mount_surface` is `wall` or `desk`. `getActiveTabletQuestions(mountSurface?)` in `questions.ts` computes the active question set; used by both `StepEnvironment` and `ConfiguratorShell` for display and the Next button gate.
- **Environment question updates** — `power_needed` question removed; `mount_surface` choices renamed: "Vehicle / forklift" → "Vehicle / Drill Down", "Pole / arm" → "Forklift / Pole"
- **StepReview HMR fix** — `useEffect` now depends on `[liveBundleOptions]` so it re-fetches whenever context resets to null (device change, HMR hot-reload). Proper `r.ok` check and `catch` block set `noProductsFound` instead of silently falling back to placeholder data.
- **Two-pass Claude AI edit** (`/api/ai-edit`) — replaces client-side keyword matching in `aiEdit.ts`. Pass 1: Claude parses free-text request into structured intent (action, component, constraints). Pass 2: filters live BC catalog by device compatibility and product type, Claude selects best candidate and returns a one-sentence reason. The **case is locked** — AI edits only swap Mount or Accessory. If the user asks to change the case, an inline guidance message appears ("go back to Step 2"). Low-confidence or no-candidate results escalate to Contact Sales. `src/lib/aiEdit.ts` is retained as a reference but is no longer imported.
- **`mount_surface` is now an array** in `ProductEnrichment` — a single mount SKU can list multiple compatible surfaces (e.g. `['wall', 'desk']`). `scoreMount` uses `.includes()` for surface matching. 10 SKUs gained `'desk'` as an additional surface: MMU104, MMU115, MNU504, MMU117, MMU332, MMU232, MMU331, MMU231, MVU332, MVU232.
- **Case-mount compatibility enforcement** — mount selection is now per-case (not shared across both bundle options). After selecting each case, the bundle route checks if it has `vesa_compatible` in enrichment features before allowing drill-only mounts. Compatibility matrix: **Bold + Extreme (HTA6024, CWM347MP)** → VESA/Drill Down + MagConnect; **Slim, Pro, Go, Edge (tablet)** → MagConnect only, no drill-only mounts. HD mounts (drill+adhesive solution_type) qualify for all series since adhesive option is always present. This prevents Slim+VESA incompatible bundles.
- **Features step label updates** — "MIL-STD-810H rated" → "Drop-proof certified" (MIL-STD-810H moved to description); "Kensington lock compatible" → "Lockable Protection"; "VESA mount compatible" → "Secure mount compatible" (VESA in description); "MagConnect compatible" → "Magnetic mount compatible" (MagConnect in description); "Screen protector included" removed from selectable list (still drives accessory scoring internally, same pattern as `hand_strap` for iPhone).
- **`vesa_compatible` and `magconnect` removed from selectable features** — both removed from `DEVICE_FEATURE_MAP` in `catalog.ts`. Mount pairing is now driven entirely by environment answers (`mount_surface` + `mount_install`) and the case-series compatibility filter. Case enrichment entries carry `vesa_compatible`/`magconnect` internally for bundle logic — they are not user-facing.
- **Contact Sales form** — `StepContact.tsx` uses a custom React form that POSTs to `/api/contact`. Sends device name + full bundle context (type/name/SKU/quantity/sub-total) in the HubSpot `message` field. HubSpot Forms Submission API — no auth token required. `src/components/ui/HubSpotForm.tsx` is retained but superseded (embed injection was unreliable).
- **HubSpot form configuration** — Portal ID `20662622`, Form ID `ba721aec-670d-456f-b004-c8434e9e3170`. reCAPTCHA must remain **disabled** on this form (it blocks the Forms Submission API). `region_name` field removed from code and HubSpot form editor. Submissions from localhost are briefly quarantined as spam — resolves automatically in production.
- **Share bundle — top-level `mailto:` hand-off** — `StepBundle.tsx`'s "Share bundle" `onClick` sets `(window.top || window).location.href` to a `mailto:` URL (subject = `Bundle recommendation for {device}`, body = device + bundle items/SKUs/qty/sub-total), so the user's mail client opens with the bundle pre-populated to send to anyone. This went through several iterations: `window.location.href` → real anchor → hidden-iframe hand-off → clipboard-copy-only, chasing a blank-popup-window symptom that turned out to be specific to one tester's enterprise-managed Chrome profile (no protocol handler, no OS default mail app — unrelated to how the app embeds). Reverted to clipboard-copy-only, but that broke entirely once tested on the **live embedded HubSpot page**: `navigator.clipboard.writeText()` is blocked by the browser's Permissions Policy inside a cross-origin iframe unless the iframe explicitly grants `clipboard-write` (the embed iframes only grant `allow="payment"`), so it was silently failing every time in the actual deployed context. Landed on `mailto:` after all, using the same `window.top` top-level-navigation fix already proven for the checkout redirect below — a mail hand-off attempted from inside the nested iframe can fail to reach the OS mail client at all, so it's targeted at the top-level page instead, which works identically whether embedded or not (`window.top === window` when not framed). Verified with Playwright in a simulated nested-iframe scenario: 0 new tabs/popups on click, no cross-origin errors writing to `window.top.location`.
- **Product page links** — in both `StepReview.tsx` and `StepBundle.tsx`, each bundle item's name is an `<a target="_blank">` and its thumbnail image has an `onClick` that both open the item's live BigCommerce product page. `src/lib/bigcommerce.ts` reads BC's `custom_url.url` (returned by default on every product, no extra `include=` needed) and resolves it against the store's default domain (`https://store-{BC_STORE_HASH}.mybigcommerce.com`) into `BcProduct.product_url`; `/api/bundle/route.ts` passes it through as `BundleItem.productUrl`. Falls back to plain non-clickable text/image when BC has no URL for that SKU.
- **Kensington lock matching — computed from SKU, not hand-curated** — `getEnrichment()` in `enrichment.ts` appends `kensington_lock` to a product's `features` whenever its SKU ends in `KL` (verified against real BC product descriptions — e.g. CWA653KL: "Built-In Security Locking Plate... compatibility for Kensington locks"). This replaced relying solely on manually-curated `PRODUCT_ENRICHMENT` entries, which live data showed were incomplete (only 2 of 5 known KL SKUs had entries).
- **IP68 certification matching tightened** — `FEATURE_TO_CERT_KEYWORD.ip_rating` in `/api/bundle/route.ts` was tightened from a loose `'IP'` substring to `'IP68'` specifically. The looser match let lower-tier ratings (IP54, IP64, IP4) falsely satisfy the "IP68 waterproof rating" feature for 7 SKUs (CWA625, CWA605, CWS301, CPA307H, CPA304H, CWM344MP, CWM404MP). Verified no regression against genuine IP68 cases (e.g. CWA4152MH).
- **Unmet-feature disclosure banner** — when the best-matching case for a user's selected features doesn't actually have certification for one of them (checked via `getUnmetFeatureLabels()` against BC `certifications` and enrichment `features`), `BundleItem.unmetFeatureLabels` is set and both `StepReview.tsx`/`StepBundle.tsx` render an amber banner ("No case with X is available for {device} — showing the closest match instead.") with an inline "Contact sales" escalation link, instead of silently showing a non-matching case as if it fit.
- **Pre-made bundle products excluded from selection** — `/api/bundle` and `/api/ai-edit` both exclude any product with `product_type` of `"Mount and Case"` (not in live use yet, kept for when it is) or `"Mount Bundles"` (29 live SKUs, e.g. the "RAM-Adapt Kit w/ aXtion Bold MP" combos and various LockDown clamp/wall mounts), plus SKU `CPA310HS` specifically (`product_type: "Cases"`, named "aXtion Edge Bundle for iPhone 15" — a pre-made case+strap bundle). This selector works by pairing individual case/mount/accessory SKUs into a bundle, so pre-assembled bundle products aren't valid candidates for any slot. Verified against live BC data: 28 legitimate `Mounts`-type products remain eligible (mount recommendations still work correctly), the 29 `Mount Bundles` SKUs no longer surface anywhere, and iPhone 15 now correctly recommends `CPA310S` instead of `CPA310HS` as its case.
- **Case-specific accessory compatibility (`compatible_case_skus`)** — accessory selection in `/api/bundle` is now per-case, not shared across both bundle options (same architectural fix already applied to mounts). New `ProductEnrichment.compatible_case_skus?: string[]` field restricts an accessory to bundles whose selected case SKU is in that list; `selectAccessoryForCase(caseSku)` filters the eligible pool by it before scoring. First use: `PCA213` (DuraLink USB-C cable, physically built for the aXtion Pro's port cover) → `compatible_case_skus: ['CWA659MP']`.
- **Fix: Tier 1/Tier 2 accessory fallback made per-case too** — the initial `compatible_case_skus` change above caused a regression: the pre-existing "device-specific accessories override universal ones" rule (Tier 1 = has `device_compatibility` listing this device, Tier 2 = universal) was still decided **once globally** per device, before per-case filtering. For a device like iPad 11" (A16) where `PCA213` (Tier 1, case-restricted to `CWA659MP`) is the only device-specific accessory, that device-wide Tier 1/Tier 2 decision locked in Tier 1 for *every* case in the bundle — so a second case like `CWA652MP` (Bold), which `PCA213` doesn't fit at all, ended up with **no accessory**, silently dropping the universal shoulder strap (`CWX202`) it should have fallen back to. Fixed by moving the Tier 1 vs Tier 2 decision inside `selectAccessoryForCase(caseSku)`: `specificAccessories`/`universalAccessories` are each filtered by `compatible_case_skus` for the case in hand first, *then* Tier 1 is preferred only if it has a non-empty result for that specific case. Verified against live BC data (iPad 11" A16): Option 1 (`CWA659MP`) still correctly pairs with `PCA213`; Option 2 (`CWA652MP`, same device) now correctly falls back to `CWX202` (Universal Shoulder Strap II) instead of showing no accessory.
- **Mobile-responsive bundle item rows** — `StepReview.tsx` and `StepBundle.tsx` item rows now stack vertically below Tailwind's `sm:` breakpoint (640px) instead of staying in the single-row desktop layout (image · name/sku/price · qty/price) that was cramming the product name into too narrow a column on real phone screens, causing broken/overlapping text. Each row is now `flex-col` by default with two grouped sub-blocks — image+details, and qty+price — that switch to `sm:flex-row` and restore the exact original desktop layout at 640px and up. Also added `break-words` on product names so long names wrap cleanly instead of overflowing. Verified with Playwright screenshots at 375px (iPhone-width — clean 2-row stacked layout, full name visible) and 900px (unchanged from before the fix) for both views.
- **Intro splash screen** — `ConfiguratorShell.tsx` adds a new `'intro'` step, shown on first load and whenever Reset is pressed (before `'devices'`). It's a standalone early return in the component — no top bar, no `StepNav` red step circles, no progress bar/card header — just a centered white card with an eyebrow ("Start Here"), a title ("Solution Bundle Builder"), and a "Get Started" button (`IconArrowRight`) that calls `setStep('devices')`. `STEP_META`'s type was narrowed to `Record<Exclude<StepId, 'intro'>, ...>` since intro has no progress/breadcrumb metadata — TS control-flow narrowing after the early return keeps `STEP_META[step]` type-safe for every other step. Verified with Playwright: renders on initial load, "Get Started" correctly advances to the Devices step, and Reset correctly returns to the intro screen after progressing partway through the flow.
- **Fix: duplicate "Back" button on Contact step** — `StepContact.tsx` renders its own internal "← Back" link below the submit button, but `ConfiguratorShell`'s generic nav footer (`showNavRow`) wasn't excluding the `'contact'` step the way it already excluded `devices`/`review`/`bundle` (each of which also handle their own back/confirm internally) — so a second, redundant "Back" button rendered below it. Found while testing Contact form submission on mobile. Fixed by adding `'contact'` to the `showNavRow` exclusion list. Verified with Playwright: exactly 1 "Back" button now renders on the Contact step, and it still correctly navigates back to the step the user came from (Features, for the certified-case path).

### Key files
| File | Purpose |
|------|---------|
| `src/lib/enrichment.ts` | **Primary recommendation control** — SKU→attribute map, runtime cache |
| `src/lib/bigcommerce.ts` | BC API client — `getAllProducts()`, `getFirstVariantIds()`; resolves `custom_url` → `product_url` for click-through links |
| `src/lib/claudeEnrichment.ts` | Batch Claude inference for unknown SKUs |
| `src/app/api/bundle/route.ts` | Live bundle builder — per-case mount selection with compatibility filter; `getUnmetFeatureLabels()` for disclosure banner; `FEATURE_TO_CERT_KEYWORD` for cert matching |
| `src/app/api/ai-edit/route.ts` | Two-pass Claude AI edit — intent parsing + BC candidate selection |
| `src/app/api/claude/route.ts` | "Why this bundle fits" reasoning paragraph — only place Anthropic SDK is called |
| `src/app/api/contact/route.ts` | HubSpot Forms Submission API — no auth token; `message` field carries bundle context |
| `src/app/api/cart/route.ts` | BC cart creation — prefers live BC IDs over `SKU_TO_BC_IDS` fallback |
| `src/app/api/admin/enrich/route.ts` | One-shot seed endpoint — POST to regenerate enrichment map |
| `src/components/configurator/StepContact.tsx` | Custom React contact form — builds `message` from bundle state |
| `src/components/configurator/StepBundle.tsx` | Bundle step — "Share bundle" is a real `<a href="mailto:...">`; item name/thumbnail link to BC product page |
| `src/lib/ConfiguratorContext.tsx` | `liveBundleOptions` state, `SET_BUNDLE_OPTIONS`, 3-priority `liveProducts`; feature/scenario/certified changes invalidate the cached bundle |
| `src/components/configurator/StepReview.tsx` | Fetches `/api/bundle` on mount; calls `/api/ai-edit` for AI edits; no-products message; unmet-feature banner; item name/thumbnail link to BC product page |
| `src/lib/questions.ts` | `ENV_QUESTIONS_TABLET` — `power_needed` removed, `mount_install` added (conditional); `getActiveTabletQuestions(mountSurface?)` exported |
| `src/lib/catalog.ts` | Feature labels updated; `vesa_compatible` + `magconnect` + `screen_protector` removed from `DEVICE_FEATURE_MAP` |
| `src/lib/aiEdit.ts` | **Superseded** — retained for reference; no longer imported |
| `src/components/ui/HubSpotForm.tsx` | **Superseded** — retained; embed approach abandoned |
| `src/types/index.ts` | `BundleItem` — `productUrl` (BC product page link), `unmetFeatureLabels` (disclosure banner) |

---

## ENRICHMENT WORKFLOW

Run when BC catalog changes (new SKUs added):

```bash
# 1. Regenerate enrichment for all current BC products
curl -s -X POST http://localhost:3000/api/admin/enrich \
  | python3 -c "import sys,json; print(json.load(sys.stdin)['typescript'])"

# 2. Paste the output into PRODUCT_ENRICHMENT in src/lib/enrichment.ts
# 3. Manually re-add mount_surface arrays, vesa_compatible/magconnect case features,
#    and bundle_priority values — the enrich endpoint does not generate these.
# 4. Commit enrichment.ts
```

### Enrichment field reference
| Field | Applies to | Values | Effect |
|-------|-----------|--------|--------|
| `mount_surface` | Mounts | Array of `wall\|vehicle\|desk\|pole\|na` | A mount can list multiple surfaces. `scoreMount` uses `.includes()` to match. BC `solution_type` is used as a secondary score for adhesive vs drill-down preference. |
| `features` | Accessories + Cases | FeatureId array | **Accessories:** scored when user selects matching features or scenario implies them. **Cases:** `vesa_compatible` gates whether drill-only mounts are allowed; `magconnect` is informational. Neither is user-selectable. |
| `series` | Cases | `Extreme\|Bold\|Slim\|Edge\|Standard\|Pro\|Go` | Used to rank cases by ruggedness fit |
| `bundle_priority` | Cases + Mounts | `1` = preferred, `2` = secondary | Tie-breaker when two products score equally. All 7 HD mount SKUs use `1`. |
| `compatible_case_skus` | Accessories | Array of case SKUs | Restricts the accessory to bundles whose selected case SKU is in this list — for accessories physically built for one specific case (e.g. `PCA213` → `['CWA659MP']`). Unset = eligible for any case. |

Valid `features` values for **accessories**: `shoulder_strap`, `hand_strap`, `screen_protector`, `kensington_lock`, `magsafe`  
Valid `features` values for **cases**: `vesa_compatible`, `magconnect` (internal use only — not selectable)

**Case-mount compatibility (enforced in `/api/bundle`):**
| Case series | VESA / Drill-only mounts | MagConnect |
|---|---|---|
| Bold | ✅ | ✅ |
| Extreme (HTA6024, CWM347MP) | ✅ | ✅ |
| Slim, Pro, Go, Edge (tablet) | ❌ | ✅ |
| iPhone (any series) | — | — (no mounts) |

HD mounts (`solution_type: ['Drill Down', 'Adhesive']`) qualify for all case series since adhesive option is always present.

**`hand_strap` note:** For iPhone accessories, set `features: ['hand_strap']` in enrichment.ts. It is NOT selectable by iPhone users — implied automatically when `carry_style` is `holster` or `hand`. For tablet devices, `hand_strap` remains selectable.

**`screen_protector` note:** Removed from selectable features in `DEVICE_FEATURE_MAP` but retained as a valid FeatureId in enrichment. Scoring in `/api/bundle` still uses it to recommend screen protector accessories automatically.

Empty `{}` entries are intentional — they mark known BC SKUs so the bundle route skips runtime Claude inference for them.

---

## DEV SERVER

Requires Node v20 via nvm:
```bash
source ~/.nvm/nvm.sh && nvm use 20.20.2 && npm run dev
```
Runs on **localhost:3000**.

`playwright` is installed as a devDependency for driving headless-browser UI verification (screenshot + console-error checks) during development. Not part of the production build.

---

## CRITICAL: Tailwind v4 CSS cascade layer gotcha

Tailwind v4 places all utility classes inside `@layer utilities`. Any CSS written outside a named layer (unlayered) sits **above** all named layers in the cascade and will override Tailwind utilities regardless of specificity.

- **Base resets (`* { margin: 0; padding: 0 }`) MUST go inside `@layer base`**, not bare in the stylesheet. Otherwise they silently zero out all Tailwind margin/padding utilities.
- **For critical layout spacing**, use a plain CSS class in globals.css (like `.page-outer`) rather than Tailwind utilities. Unlayered class selectors beat `@layer base` resets and are immune to layer conflicts.
- Do NOT write bare `*, body, html` rules outside a layer — they will break spacing utilities globally.

There is no `tailwind.config.ts`; brand colors are registered via `@theme` in globals.css (Tailwind v4 pattern):
```css
@theme { --color-brand: #c8291c; --color-brand-hover: #a8221a; --color-share: #534AB7; }
```

---

## CRITICAL RULES

1. **Claude API is server-only.** The Anthropic SDK must never be imported in any client component. Only `src/app/api/claude/route.ts` and `src/app/api/ai-edit/route.ts` call it.

2. **No `use client` on API routes.** All files in `src/app/api/` are server-side by default.

3. **Prototype is the UI source of truth.** If there is any ambiguity about layout, spacing, color, or interaction behavior, open `configurator_v16_final.html` and match it exactly.

4. **All 5 steps plus contact must work end to end.** The configurator must be fully navigable from device selection through the bundle view with all three CTA actions (Add to cart, Contact sales, Share bundle) wired up.

5. **AI edits are server-side only.** `/api/ai-edit` handles both Claude passes and the BC query. The case is always locked — AI edits only swap Mount or Accessory. `src/lib/aiEdit.ts` is retained for reference but is no longer the active implementation.

6. **TypeScript strict mode.** No `any` types. All component props must be typed.

7. **Tabler Icons via `@tabler/icons-react`.** Use the React component API: `import { IconShield } from '@tabler/icons-react'`. Icon names map from `ti-*` classes by converting to PascalCase with `Icon` prefix (e.g. `ti-shield` → `IconShield`).

8. **BigCommerce REST only — no GraphQL.** All BC fetches go through `src/lib/bigcommerce.ts`. Never call BC directly from components.

9. **Case SKU is always the anchor for AI swaps.** When swapping Mount or Accessory, always inherit `device_compatibility` and certifications from the current case's custom fields.

10. **`product_status: "Request for Quote"` is a hard fallback trigger.** Exclude RFQ products from bundle and AI selection; trigger contact sales if no other candidates exist.

---

## BIGCOMMERCE CATALOG — CUSTOM FIELD REQUIREMENTS

The bundle logic in `/api/bundle/route.ts` is only as accurate as the custom field data in BC. See `docs/BUILD_REFERENCE.md` for detailed analysis of each priority. Summary:

| Custom Field | Applies To | Values | Impact |
|---|---|---|---|
| `mount_surface` | Mounts | `wall\|vehicle\|desk\|pole\|na` | Eliminates mount keyword fragility |
| `features` | Accessories | `shoulder_strap\|hand_strap\|screen_protector\|kensington_lock\|magsafe` | Direct feature→accessory mapping |
| `series` | Cases | `Extreme\|Bold\|Slim\|Edge` | Enables feature-driven case ranking |
| `bundle_priority` | Cases + Mounts | `1\|2\|3` | Controls Option 1 vs Option 2 ordering |
| `features` | Cases | same FeatureId vocabulary | Scores cases against user preferences |
| `device_compatibility` | Cases | exact device name strings | Must match configurator device list exactly |
| `product_status` | All | `Request for Quote` | Triggers contact sales path for RFQ items |
